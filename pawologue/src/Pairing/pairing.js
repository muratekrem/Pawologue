import React from 'react';
import Navbar from '../Navbar';

const pairing = () => {
    return(
        <div>
            <Navbar/>
            PAİRİNG
        </div>
    );

}
 export default pairing;